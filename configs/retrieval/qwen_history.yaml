strategy: qwen_history_state
model_path: ${QWEN_MODEL_PATH}
model_revision: ${QWEN_MODEL_REVISION}
worker_python: ${QWEN_PYTHON}
index_path: ${QWEN_HISTORY_INDEX_PATH}
candidate_k: 1
image_size: 224
embedding_dim: 2048
batch_size: 8
timeout_seconds: 300
history_length: 8
history_weight_power: 1.0
instruction: Represent the PushT scene and the supplied simulator state for matching demonstrations.
